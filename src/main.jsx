import React from "react";
import ReactDOM from "react-dom/client";
import HRMemoSystem from "./HRMemoSystem";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <HRMemoSystem />
  </React.StrictMode>
);
